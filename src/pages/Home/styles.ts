import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  containerHome: {
    width: '100%',
  },
  contentsHome: {
    backgroundColor: '#fff',
    borderBottomEndRadius: 25,
    padding: 25,
    paddingBottom: 80,
  }
});

